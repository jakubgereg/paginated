{
  "name": "@jgereg/paginated",
  "version": "0.0.1",
  "description": "Lightweight utility package designed to dynamically render React components based on provided templates.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jakubgereg/paginated.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/**.*"
  ],
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write '**/*.{ts,tsx,js,html}'",
    "format:check": "prettier --check '**/*.{ts,tsx,js,html}'",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "check": "npm run format:check && npm run typecheck && npm run test",
    "preversion": "npm run check",
    "postversion": "git push && git push --tags && npm publish --access public",
    "prepublishOnly": "npm run build",
    "test": "jest"
  },
  "keywords": [],
  "author": "Jakub Gereg",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "29.5.5",
    "jest": "29.7.0",
    "prettier": "3.2.4",
    "ts-node": "10.9.2",
    "ts-jest": "29.1.2",
    "typescript": "5.3.3"
  },
  "dependencies": {},
  "bugs": {
    "url": "https://github.com/jakubgereg/paginated/issues"
  },
  "homepage": "https://github.com/jakubgereg/paginated#readme"
}
